# Kivio Desktop v2.9.11 — 测试版 / Pre-release

桌面 AI 助手 · macOS (Apple Silicon) + Windows。Desktop AI assistant for macOS (Apple Silicon) and Windows.

此版本用于测试。请在使用中反馈遇到的问题。This is a pre-release for testing and feedback.

## 下载 / Downloads

- **Windows 安装包 / Installer** — `Kivio.Desktop_2.9.11_x64-setup.exe`（NSIS）
- **Windows 便携版 / Portable** — `Kivio.Desktop_2.9.11_x64-portable.zip`，解压后运行 `Kivio Desktop.exe` / unzip and run.
  - 需要 Edge WebView2，设置和对话与安装版共用。Requires Edge WebView2; settings and conversations are shared with the installed app.
- **macOS** — `Kivio.Desktop_2.9.11_aarch64.dmg`（Apple Silicon，未签名 / unsigned）
  - 首次启动：右键 → 打开，或运行 `xattr -cr "/Applications/Kivio Desktop.app"`。
  - First launch: right-click → Open, or run the command above.

## 本次变化 / What's New

以下整理 **v2.9.10 → v2.9.11** 的主要变化。These are the main changes since v2.9.10.

- **作品库 / Works library** — 聊天里交付的文档、图片、表格等收入独立作品页，支持预览、重命名、删除和多选另存；删除只从作品页拿掉，不改聊天和原文件。源对话删除后对应条目一并消失。Chat-delivered documents, images and spreadsheets land in a Works page with preview, rename, delete and multi-select export. Removing an item hides it from Works without changing the chat or source file; deleting the source conversation drops its gallery entries.
- **侧栏对话 / Sidebar conversations** — 归档后仍打开的对话留在侧栏；往归档对话里继续发送会重新启用它，新对话也不会复用已归档的空白会话。An archived conversation stays in the sidebar while it is open; sending into it un-archives the thread, and new chats no longer reuse archived blanks.
- **生图尺寸 / Image generation** — `mixer_generate_image` 支持 512 / 1K / 2K / 4K、自定义像素和官方比例，工具描述里带上当前模型和调用示例。The image tool accepts 512 / 1K / 2K / 4K, custom pixels and official ratios, with the configured model and call examples in the description.
- **GitHub 连接 / GitHub connector** — 内置 GitHub 设备码授权，不必再手填 token。Built-in GitHub device authorization, so a personal token is no longer required.
- **文件工具 / File tools** — 工具说明和实际权限对齐；旧工具名仍转到现名；拖入文件夹按系统路径附加；续读提示放在正文后面。Align file-tool guidance with actual access, route renamed tool names, attach dropped folders by native path, and put read-continuation notices after the content.
- **工具结果 / Tool results** — 保持工具结果顺序，并改善中断后的恢复用语。Preserve tool-result order and recovery language after interruptions.

完整变更 / Full changelog: https://github.com/ZMGID/kivio/compare/v2.9.10...v2.9.11
