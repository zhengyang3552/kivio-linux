# 回退上下文优化，仅保留实报与短回执

用户要求：整体回退此前连续优化，仅重新实现上下文实报优先和去掉文件操作重复回显。此次取 `55b1dd36`（这轮优化之前）为行为基线；以新提交记录回退，不重写Git历史，不覆盖已有未提交功能。

## 仅保留两项

1. 上下文计数：有有效接口实报时使用实报，即使字符估算更大也不得覆盖它；没有实报才整体估算。只对实报之后未计入的新增消息估算，并明确标注混合来源。实时事件与重新读取会话使用同一选择规则；估算推理正文不重复计算展示副本或密文。沿用基线原有的清空/压缩边界失效行为，不引入请求指纹或新的输出预算机制。
2. 文件操作回执：成功写入/编辑只向模型返回简短摘要和警告，不重复回显代码diff。完整diff仍留在原有审阅数据；磁盘正文和历史工具参数保持原样。失败保留原有诊断回显。

## 回退范围

相对基线，除上述计数实现及其协议/界面来源标记、回执判断和必要测试外，不保留这轮新增的执行行为。删除check模式、执行/浏览器/结束策略提示、历史参数省略、实报请求指纹、新输出预算、shell预览改写及文件diff/落盘策略变更。这是用户选择的整体回退，不将先前独立修复再悄悄混回。原研究和测试文档在Git历史中保留，当前规格以本文为准。

## 验证要求

- 实报小于估算时仍用实报；无实报兜底；实报后增量及来源标签正确；原有重开会话锚点测试通过。
- 真实文件write/edit结果保持完整，成功回执不含代码，警告与完整审阅diff保留。
- 全量测试、类型和生成协议检查；区分已有平台失败，不宣称整题评分或耗时已恢复。
- 检查最终暂存树相对`55b1dd36`只含本规格内容，原有工作区修改仍保持未暂存。

## 验证结果（2026-09-14）

前端207个测试文件、1604项用例通过；TypeScript和`protocol:check`通过。Rust完整测试2385通过、2失败、45忽略；失败仅为此前已有的两个Windows下Unix/macOS路径断言（`path_env::tests::common_dirs_macos_expands_home`、`path_env::tests::merge_unix_from_minimal_path_adds_common_dirs`）。实报优先、实时事件、会话锚点及真实write/edit回执测试均通过。Standards与Spec复审剩余发现均为0。

8个核心执行/工具/文件/请求文件已核对与`55b1dd36`完全一致；原有未提交修改逐文件增删行数与备份一致，唯一冲突保留了原有规划工具插入。开发应用重新编译启动。未重跑付费模型整题，不能据此宣称评分或耗时恢复。
