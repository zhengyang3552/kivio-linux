# 上下文优化范围与最新结果复核

本次按用户对连续退步的反馈，只读复核提交历史、已保存的同题会话和裁判结果；没有修改产品代码、参赛成果或运行新的付费模型测试。当前提交为 `7f1030c4`。

## 原目标与实际结果

原目标有三项：减少写入/编辑的重复回显；减少无必要的整文件重写、大段参数和返工；上下文统计优先采用接口实报、无实报才估算。最终应以交付质量、时间及消耗共同验收，不能只看最后上下文长度。

| 指标 | 最初完成轮 | 最新完成轮 |
| --- | ---: | ---: |
| 裁判评分（同一75分口径，不计UI和时间） | 70 | 62 |
| 首条用户消息至主回复完成 | 19分33秒 | 27分30秒 |
| 最终实报上下文 | 269150 | 228657 |
| 历史中assistant请求条数 | 94 | 125 |
| 工具调用 | 107 | 135 |
| 累计输出token（含推理） | 181690 | 177262 |
| write/edit回执字符数 | 92334 | 3932 |
| write/edit调用数 | 14/30 | 22/39 |
| 工具duration_ms之和 | 361.343秒 | 810.006秒 |

上下文减少40493（15.0%）；耗时增加477秒（40.7%）；累计输出只减少2.4%。工具时长之和增加448.663秒，但并行工具时长可能重叠，不能把求和当成精确互斥墙钟分解。

最初会话：`conv_c88caab1-0213-41b8-ab99-aae1959544a9`。最新会话：`conv_cab5be08-33c2-4698-b9a3-157697e3a0b3`，只计算第一个主回复，排除后来追加对话。保存位置为本机 `AppData/Roaming/com.zmair.kivio/conversations`。

最新裁判报告：`E:/ZM database/examkivio/_judge/reviews/20260914_135952_kivio_rerun/RESULT.md`。评分依次70、67、62。本轮关键路径顺序及平局处理、相邻工作窗错误合并、取消依赖和状态/畸形输入验证有失分。独立运行的92个非UI自测全绿仍未覆盖这些错误，不能把自测数量当作正确性证明。

## 实际改动清单

| 改动 | 提交/位置 | 状态和评价 |
| --- | --- | --- |
| 实报优先、估算去重 | `4e3141a2`，后续`8697a218`、`fa684f6e`修正 | 属于原问题，应保留。显示值不是上下文内容优化，不能宣称使模型更快。 |
| 成功文件回执精简，完整diff留给审阅 | `5cead647`，`mcp/registry.rs` | 属于原问题，回显减少95.7%有记录支持；不能据此推算全部token收益。 |
| 局部编辑提示、较短工具描述 | `5cead647`、`62877d31`、`e61b15d4`，`prepare.rs`、`mcp/types.rs` | 属于原方向，但未证明降低返工；最新write/edit次数增多。不能由调用次数单独断定都是冗余。 |
| diff算法、准确行数、Windows原子替换、并发修改检查 | `5cead647`，`native_tools/files.rs` | 包含修改可信度和独立正确性修复，不等于生成代码变少；不应为回退行为策略而顺带撤销这些修复。 |
| 输出预算、实报请求指纹、压缩入口调整 | `5cead647`、`62877d31`，后续`fa684f6e`修正 | 扩大了最初优化范围。曾导致有效实报被丢弃并回退估算；已修复显示与预算状态混用。没有证据证明它们带来本题收益。 |
| 改写历史write/edit参数为省略占位符 | `e61b15d4` | 确认的严重回归：上一轮占位文字进入新write并实际写盘，引发重建测试。`fa684f6e`已删除此机制，禁止恢复。 |
| 工具结果与图片顺序、中断折叠 | `964badf8`、`04ca7ce3` | 后续独立故障修复，应区别于上下文节省策略。 |
| shell长输出预览改为2KiB头+6KiB尾、保留完整日志 | `5cead647` | 超出文件回执的反馈变化；最新轮未见完整日志外置回执，不能将本轮变慢归因于它截掉诊断。 |
| 显式check模式、每次检查日志、浏览器探测/验证复用/结束提示 | `7f1030c4` | 扩展到执行策略。真实工具入口单测通过，但最新轮check参数使用次数为0，重复全量验证仍发生，未达到预期端到端效果。 |

## 最新轮直接证据

1. 第5、6轮探测并成功使用Playwright；本轮不能继续套用上一轮“没有探测浏览器工具”的解释。
2. 本轮135次工具调用没有旧历史占位参数；不存在再次出现已知占位污染的记录。
3. `check:true`使用0次。失败测试仍通过普通bash接tail，例如第63轮UI测试3失败、10通过，工具仍报告exit_code:0。新功能存在不等于模型已使用；未保存的完整原始请求不作收到新版schema的逐字证据。
4. 第108轮106个测试全部通过，54.245秒。第109轮中间没有代码修改，逐文件再跑同一批测试，只为打印各文件计数，57.994秒。这次重复有明确记录，不把其他修改后的复测一概认定为浪费。
5. 第104、108、121轮也运行全量测试，但它们之前有相关代码/UI修改，应分别评估必要性，不能全部累计成“无变化重跑”。

## 应收回的结论与后续边界

此前把短回执和局部回归测试通过，当成整个Agent已改善，是验收方法错误。当前数据支持“回显冗余明显减少”，不支持“生成效率提高”或“综合能力恢复”。参数历史占位符引发污染是已证实代码因果；当前62分的每个业务错误是否由某条提示造成，尚无单变量对照，不能编造归因，也不能推给模型作为结论。

下一步应收缩到可审阅的最小基线：保留实报优先、简短成功文件回执、完整审阅diff及独立正确性修复；停止继续叠加执行提示和参数精简。若撤回执行策略，精确撤回`7f1030c4`相关策略及其他未证实收益的行为改动，另行检查依赖，不整仓回退、不恢复占位参数、不覆盖其他工作区修改。历史污染之后的版本不是可靠的“优化前基线”。

验收要比较相同题目、模型/强度、初始材料和裁判口径；先过质量底线，再比较时间和token。不再用本地模拟模型的单元测试替代真实任务行为验证。本次没有执行这样的受控模型A/B，因此没有宣称已经修好分数下降。

指标提取命令已运行：`python .scratch/issue-51/audit-latest-scope.py`，结果存于同目录`scope-audit-metrics.json`及`latest-command-audit.json`。它是保存记录的可重复统计，不是复现模型评分回归的测试；目前没有能在几秒内确定重现整题生成质量下降的测试入口。
