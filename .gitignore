# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

dist-electron
release
src-tauri/target
# rust-analyzer 在 workspace 根生成的 cache,不是 cargo 用户产物
/target

# Local build artifacts (OCR helper / leftover sandbox downloads)
resources/ocr
resources/python-sandbox/
.cache/swift

# Swift sidecar 编译产物 (npm run build:swift / CI 生成,不入库)
src-tauri/binaries/
src-tauri/swift/kivio-ai-helper/.build/
src-tauri/swift/kivio-ocr-helper/.build/

.superpowers/

# CodeGraph 本地索引（生成物 / 本地缓存，不入库）
.codegraph/

# AI agent 工具脚手架（各 CLI / Trellis 生成的本地配置与依赖产物，不入库）
.agents/
.claude/
.codex/
.cursor/
.opencode/
.trellis/

# AI 助手项目记忆（本地维护，不入库）
MEMORY.md

# Local runtime discovery caches and manual E2E artifacts
/mcps/
/output/
/.playwright-cli/

# 本地手测用的外部仓库克隆（不入库）
/test-ZMGID/
